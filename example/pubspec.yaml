name: cdx_comments_example
description: Example app demonstrating the cdx_comments package
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  cdx_comments:
    path: ../
  provider: ^6.1.5+1

dependency_overrides:
  intl: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

